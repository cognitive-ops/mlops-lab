# API port exposed by docker compose
API_PORT=8080

# Directory where model.joblib and metadata.json are written / read
ARTIFACTS_DIR=artifacts

# MLflow — omit or leave empty to skip experiment tracking
MLFLOW_TRACKING_URI=
MLFLOW_EXPERIMENT=iris-classifier
RUN_NAME=random-forest
