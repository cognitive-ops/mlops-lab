name: onnx-env
channels:
  - defaults
  - pytorch
  - conda-forge
dependencies:
  - python=3.10
  - pytorch
  - torchvision
  - torchaudio
  - cpuonly  # Remove this if you want GPU support
  - pip
  - pip:
      - onnx
      - onnxruntime