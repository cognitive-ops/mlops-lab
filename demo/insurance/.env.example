# OpenAI API key used by the extractor and finalize LLM calls
OPENAI_API_KEY=sk-...

# Optional: path to the tesseract executable, if it's not on PATH.
# Only needed for the OCR fallback (scanned/image-only PDF claim docs).
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
